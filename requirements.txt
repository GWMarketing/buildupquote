# Step 1 -- parsing engine
#
# pdfplumber does the actual PDF -> text extraction (see scope_parser/extract.py).
# It's built on pdfminer.six and was already available in this environment;
# PyMuPDF/fitz would work just as well if you ever prefer it, but there was
# no reason to add a second dependency that does the same job.
pdfplumber>=0.11

# Step 2 -- the editing workspace
# The core logic lives in workspace.py -- plain pandas and Python, no
# framework. The FastAPI deployment (fastapi_app.py) and the tests both
# import it.
pandas>=2.0

# Step 2b -- FastAPI deployment (fastapi_app.py, the Hostinger VPS)
# python-multipart is required for the PDF upload endpoint (UploadFile).
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# Step 2c -- PostgreSQL + user models (CRM groundwork)
# Backed by app/database.py, app/models.py, app/schemas.py. pydantic[email]
# pulls email-validator for EmailStr; pwdlib[argon2] is the password
# hasher the auth endpoints will use (argon2). Alembic is installed and
# ready for migrations; tables are created on startup via
# Base.metadata.create_all for now.
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
alembic
python-jose[cryptography]
pwdlib[argon2]
pydantic[email]

# Step 2d -- contact import (clients importer)
# vobject parses real-world Apple/Android .vcf exports robustly (structured
# ADR, multi-valued fields). The pure-Python fallback in
# app/services/contact_import.py keeps import working even if it's missing.
vobject>=0.9.9

# Step 2e -- Stripe subscriptions (landing page checkout, webhooks, portal)
# Drive it from env vars: STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET and one
# STRIPE_PRICE_{TIER}_{INTERVAL} id per plan (see app/routers/billing.py).
# Unset keys cleanly disable the feature (503) rather than failing hard.
stripe>=9.0

# Step 3 -- branded proposal PDF export (proposal/)
jinja2>=3.1
weasyprint>=60

# WeasyPrint is the PDF renderer -- pure pip, no system-level installer
# (unlike wkhtmltopdf, whose Homebrew cask was removed). See
# proposal/render.py.

# Step 4 -- tests only (not used by the app itself)
#
# reportlab generates the synthetic PDF that tests/test_metadata.py's
# ParsePdfSurfacesContainerMetadataTest parses end-to-end, to prove the
# file's own Creator/CreationDate/Title survive the round trip. It is
# NOT a runtime dependency -- the app never imports it. Keep it listed
# here so a fresh `pip install -r requirements.txt` gets a green test
# suite instead of a missing-module error at setUpClass.
reportlab>=4.0

# Everything else here is Python standard library -- nothing else to
# install.
#
# Tests use Python's built-in `unittest`, not pytest (pytest can run
# unittest-style test files too).
